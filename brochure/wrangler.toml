# Cloudflare Pages configuration for the AI QA Tester brochure site.
# Deploy with:  wrangler pages deploy dist --project-name=ai-qa-tester-brochure

name = "ai-qa-tester-brochure"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-01"
